<Page
    x:Class="App1.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView>
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" FontWeight="Bold" FontSize="14">
                Application:
            </TextBlock>
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" FontWeight="Bold" FontSize="14">
                Background Color:
            </TextBlock>
            <Button Padding="0">
                <Rectangle Height="50" Width="50">
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{x:Bind myColorPicker.Color, Mode=OneWay}"/>
                    </Rectangle.Fill>
                </Rectangle>
                <Button.Flyout>
                    <Flyout>
                        <ColorPicker x:Name="myColorPicker"
                 ColorSpectrumShape="Ring"
                 IsColorPreviewVisible="False"/>
                    </Flyout>
                </Button.Flyout>
            </Button>
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" FontWeight="Bold" FontSize="14">
                Text Color:
            </TextBlock>
            <RadioButton IsChecked="True">Light</RadioButton>
            <RadioButton>Dark</RadioButton>
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" FontWeight="Bold" FontSize="14">
                Tile Text:
            </TextBlock>
            <CheckBox>On Small Tile</CheckBox>
            <CheckBox>On Medium Tile</CheckBox>
            <CheckBox>On Big Tile</CheckBox>
        </ListView>
    </Grid>
</Page>
